# История изменений документации KTOYA

Версия документа: 1.5
Статус: действующий журнал изменений документации.

---

# [Не выпущено]

## 05.09.2026 — голосовое AI-ядро, доказательные связи и будущая социальная сделка

### Подтверждено
- разговорный AI становится основным интерфейсом создания книги, а ручной текстовый путь сохраняется;
- большой голосовой/текстовый материал делится на подтверждаемые кандидаты: новая история, дополнение или связь для графа вопросов;
- возрастные маршруты задают адаптивные вопросы, ответы на которые складываются в сюжет, а не в анкету;
- whole-book выводы обязаны показывать истории-основания, уверенность и альтернативное объяснение; прогнозы допускаются только как условные сценарии;
- социальная сеть остаётся следующим слоем после авторского ядра: поиск людей/книг, связи историй, реакции и комментарии;
- заказная история рассматривается как будущая гарантированная сделка по согласованному ТЗ; 5% — целевой ориентир комиссии, требующий отдельной экономической и юридической проверки.

### Изменено
- `03-DECISION_LOG.md` — добавлены D057–D061;
- `05-PRD.md` — добавлены требования свободного источника, сюжетных вопросов, доказательных выводов и разделения разговорного AI/TTS;
- `09-METHODOLOGY.md` — описаны сегментация рассказа, возрастные маршруты и переход от событий к карте развития;
- `10-SOCIAL-ARCHITECTURE.md` — добавлены структура поиска/просмотра, заказ истории, гарантийный контур и комиссия;
- `11-AI-ARCHITECTURE.md` — уточнены основной интерфейс автора, `StoryCandidate`, evidence-based whole-book analysis, проводник читателя и безопасное подключение GPT/LLM;
- `02-ROADMAP.md` — новые требования размещены на этапах Умной книги, Карты человека и Социальной сети без расширения первой Beta.

### Не выполнено и не утверждено как готовая функция
- живой GPT/LLM и денежный контур не считаются подключёнными самим наличием интерфейса прототипа;
- API-ключи, платные вызовы, платежи, merge и production-размещение не выполнялись; документационный пакет публикуется только в отдельной ветке и PR и не становится каноном до merge.

## 05.09.2026 — локальный Stage A: native voice QA и восстановление

- Добавлен изолированный Playwright/Chromium fake-microphone контур с offline WAV, reload/playback/provenance и 105-секундной проверкой сохранённого оригинала.
- Исправлены lifecycle recording, sticky manual transcript, ownership дополнений, startup version и AI/autosave races. Свежие свидетельства и ограничения: [18 — acceptance](18-PR12-AI-ACCEPTANCE-MATRIX.md#6-локальный-stage-a--05092026).
- Нет push/merge, production migration, изменений IAM или новых платных вызовов.

## 04.09.2026 — повторная проверка Draft PR #12 после обрыва питания

### Исправлено
- Deterministic interview больше не повторяет ту же смысловую категорию после безопасной серверной формулировки вопроса: сохранённая `category` стала частью provider context.
- Длинный исходник не вставляется целиком в вопрос: anchor ограничен первой содержательной фразой и безопасной длиной.
- Точный неответ `Не помню` / `Не знаю` / `Затрудняюсь ответить` сохраняется как исходный материал и provenance, но не используется как следующий anchor и не включается в читаемый текст истории.
- Привязка созданной revision к `ai_operations` дополнительно ограничена аутентифицированным `user_id`, поэтому клиентский `operationId` не может изменить чужую audit-запись.
- Заголовок книги правильно склоняет количество историй (`20 историй`, а не `20 истории`).

### Фактическая проверка
- 23 test files / 101 test, ESLint, TypeScript `--noEmit`, production build и `git diff --check` прошли.
- В браузере пройдены question → non-answer → next category → assembly → Apply → rephrase → Apply → Undo → reload → exact patch → Keep → book-state; console errors/warnings отсутствуют.
- Визуально проверены 360, 390, 768, 1024 и 1600 px. Live microphone не имитировался без синтетического virtual-mic fixture.
- Security diff review охватил 30 executable/source-файлов; найденный owner-scope дефект исправлен и закреплён regression test, выживших reportable findings нет.

## 23.08.2026 — PR #12: provider-independent AI Story Core

### Добавлено
- Динамическое интервью `ASK / READY`, сборка истории по текущим sources, preview `Сейчас / Предлагается`, явные Apply/Keep, exact-match patch и append-only Undo.
- Заменяемые Alice AI и deterministic providers со строгими schemas, повторной server-side validation, current-story isolation и безопасной серверной формулировкой вопроса.
- Аддитивный D1 budget ledger с reservation до provider request, отдельными `completed / failed / uncertain`, запретом автоматического повтора неопределённых платных операций и лимитами 60/100 ₽.
- Закрытый localhost-only QA gate для новых неперсональных материалов; реальные и legacy-истории внешнему LLM не разрешены.
- Server-side gate дополнительно требует защищённый QA user ID и loopback hostname: пользовательский AppState-флаг не может самостоятельно открыть внешний provider.

### Фактическая проверка
- На синтетических сценариях проверены полноценный, слабый и противоречивый рассказ, «не помню», длинный хаотичный текст, ранняя сборка, rephrase, patch и Undo.
- Trial использовал 17 provider requests: 14 completed, 2 known failed и 1 uncertain; budget-accounted total 10,581802 ₽. Автоматических платных retries не было.
- Production migration не запускалась; публичная Beta и обработка реальных личных историй этим PR не разрешены.

## 23.08.2026 — human TTS acceptance и Beta default `marina`

### Изменено
- `marina` выбрана Автором как конфигурируемый default русского TTS текущей Beta. Runtime использует `KTOYA_SPEECHKIT_DEFAULT_TTS_VOICE` и отвергает неизвестное значение вместо неявной подстановки; при отсутствии переменной действует подтверждённый fallback `marina`.
- Другие проверенные голоса остаются доступными для сравнения, а ранее созданные `StoryNarration` не изменяются при будущей смене default.

### Фактическая human QA
- На одном неперсональном тексте длиной 1066 символов сравнены `marina`, `jane`, `dasha`, `julia`, `alexander`, `kirill`; все шесть assets сохранились после reload и имеют длительность 1:14–1:23.
- Автор подтвердил `marina` как лучший из проверенных: естественный и чистый, немного грубоватый, но приятный и неутомляющий. Последняя контрольная фраза длинной озвучки прозвучала полностью, тихого обрыва нет.
- Это Beta acceptance, не утверждение идеального постоянного голоса: следующий поиск направлен на более мягкий/нежный женский голос без потери чистоты дикции и, отдельно, на будущий голос Автора.

## 23.08.2026 — ограниченный Yandex SpeechKit trial в PR #11

### Решено и реализовано до платных запросов
- Добавлено решение D054: Yandex SpeechKit разрешён только для новых неперсональных QA-материалов, с отдельным временным service account, двумя least-privilege ролями, двумя explicit API-key scopes и коротким сроком действия.
- Browser WebM/Opus остаётся immutable original; отдельный mono PCM16 WAV 16 kHz хранится как `DerivedAudioAsset` для STT и не заменяет исходник.
- Серверный privacy gate блокирует старые/личные истории до чтения R2; canonical STT всегда использует `literatureText: false`.
- Перед каждым потенциально платным вызовом создаётся уникальная D1 budget reservation. Повтор или неопределённый исход не может незаметно создать второй запрос.
- Публичный план trial рассчитан в 12,4508 ₽ с двукратным запасом; рабочий cap — 25 ₽, абсолютный пользовательский предел — 500 ₽. Первый вызов всё ещё заблокирован до readback тарифа конкретного billing account.
- Для одной `StoryRevision` можно сохранить и сравнить отдельные narration assets шести русских голосов; финальный выбор остаётся за Автором.

## 23.08.2026 — production voice foundation после PR #10

### Реализовано без платного provider
- Разделены browser live fallback и production `TranscriptionProvider`: сохранённый `AudioFragment` обрабатывается независимым submit/poll job, а каждая ошибка и повторная попытка сохраняются append-only без изменения оригинала.
- `TranscriptRevision` и `TranscriptionAttempt` теперь живут уже в persisted capture-draft. Production STT можно повторить после надёжной загрузки аудио до сборки истории; raw/manual/production revisions и их идентификаторы переносятся в `Story` без потери `questionId`, `StorySource` или provenance.
- Production `TTSProvider` возвращает реальный аудио-asset для точной `StoryRevision`; готовый файл хранится в R2 и проигрывается стандартным seekable media player. Правка текста сохраняет старый файл как историческую версию, но не выдаёт его за актуальный.
- Media API поддерживает byte ranges для pause/seek/replay/reload; capability endpoints и ошибки не раскрывают provider payload или секреты.
- Зафиксировано решение D053 и актуальное provider research. Рекомендуется ограниченный Yandex SpeechKit trial; подключение, billing и секреты ждут отдельного разрешения Автора.

### Проверено до provider decision
- Targeted regression: 40 тестов для capture/book transcription, retries, question provenance, StoryRevision update, TTS cache/stale/error, HTTP adapters и data safety.
- TypeScript typecheck проходит. Полный test/lint/typecheck/build/diff-check будет выполнен один раз после фактического provider integration, как требует граница этапа.

### Остаётся launch blocker
- Реальная production STT-приёмка на короткой/1–2-минутной русской речи и реальная human TTS-приёмка ещё невозможны без разрешённого provider/API.
- Browser MediaRecorder в Chrome сохраняет WebM/Opus, а shortlist providers документирует WAV/OGG/MP3/FLAC, но не WebM; до production trial нужен подтверждённый input path или server-side conversion.

## 23.08.2026 — PR #10: обратимое архивирование вместо удаления

### Изменено в PR #10
- Удалена пользовательская кнопка «Удалить аудио». Единственное действие с аудио — обратимые «Убрать аудио из отображения» и «Вернуть аудио в отображение»; оно сохраняет R2-оригинал, сырой и ручной transcripts, text revisions и provenance.
- Старый локальный маркер `uploadStatus: deleted` при загрузке состояния мигрирует в честное архивное состояние с тем же object key, а не продолжает изображать удалённый материал. Регрессионные тесты покрывают архивирование, восстановление и миграцию legacy-состояния.
- Зафиксировано решение D052: история и исходные материалы автора не имеют destructive UI path; будущая архивация истории должна быть восстанавливаемой.

## 23.08.2026 — bounded-pass переходов PR #10

### Изменено в PR #10
- Удалён оставшийся недостижимый маршрут `#method`: после первого выбора Автор сразу попадает на единый экран «Рассказать», а помощь с воспоминанием сразу показывает текущий вопрос без промежуточного экрана «Рассказать об этом».
- Добавлена компактная матрица `16-PR10-STATE-TRANSITION-MATRIX.md`: она отделяет свежие доменные PASS от реальной браузерной проверки, которую нельзя подменять скрытой автоматизированной вкладкой.
- UX-документ приведён к D052: у оригинального аудио нет даже «мягкого удаления», только обратимое скрытие из пользовательского отображения.

## 23.08.2026 — PR #10 после реальной Chrome QA остаётся непринимаемым

### Изменено в PR #10
- Review-версия незавершённой истории теперь сохраняется внутри capture-draft отдельно от аудио, исходных transcript и ответов. Reload на `#draft` восстанавливает проверяемый текст и его provenance; если из незавершённого материала невозможно собрать историю, Автор безопасно возвращается в capture, а не получает пустой экран. Ручная правка review-версии также входит в это автосохранение до явного добавления истории в книгу.
- Для фрагмента без доступного URL интерфейс больше не создаёт `<audio src="">`: вместо предупреждения Chrome и попытки загрузить страницу как медиа он честно сообщает, что аудио сейчас нельзя воспроизвести в этой вкладке, сохраняя расшифровку и видимость статуса загрузки.
- Исправлена гонка lifecycle записи: поздний `SpeechRecognition.onend` мог вернуть уже загруженный оригинал в `pending`, стереть его `objectKey` в capture-draft и навсегда заблокировать «Уточняющие вопросы» после reload. Теперь поздний результат изменяет только transcript/статус распознавания, не перезаписывая успешную загрузку. При чтении и записи state сервер безопасно восстанавливает такой pending-фрагмент только когда его точный R2-оригинал действительно существует; иначе текст и pending-статус сохраняются без выдуманного файла.
- Реальная Chrome-запись 1:19 выявила обрезание финального контрольного фрагмента. После stop сначала flush-ится `MediaRecorder`, оригинал немедленно сохраняется со статусом `processing`, а поздние final results Chrome обновляют тот же фрагмент до `onend`. Если окончания нет минуту, результат отмечается как `incomplete` вместе с оригиналом, а не как полный. Последующая пользовательская проверка полной записи 102 секунды подтвердила устранение обрыва.
- Та же пользовательская проверка установила, что качество Chrome SpeechRecognition недостаточно для публичной Beta: остаются ошибки слов и почти нет пунктуации. Поэтому browser результат — только `raw`/`unverified` fallback, а production-quality STT через `TranscriptionProvider` — обязательный launch blocker. Будущая обработка создаёт отдельную `improved`-ревизию с прямой ссылкой на raw-версию, не уничтожая исходный текст, аудио или цепочку вопроса.
- Последующий ручной тест без контрольных фраз обнаружил ещё один дефект сохранности: запись, оставшаяся до «Собрать историю», исчезала из UI после reload/dev-server restart, потому что жила только в React state. Теперь отдельный capture-draft сохраняет исходные метаданные, transcript и вопросные связи до сборки истории; после reload сохранённое R2-аудио снова воспроизводится через авторизованный endpoint.
- Голос и текст объединены на экране «Рассказать»; на вопросе textarea и микрофон доступны вместе. После ответа раздельно названы «Ответить на следующий вопрос» и «Отправить историю в книгу».
- Внутренние переходы и Back браузера хранят текущий логический экран без скрытого `resetStoryFlow`.

### Не является готовностью Beta
- Результат исправления ещё требует фактической Chrome-проверки записи 60–90 секунд с контрольными фразами в начале, середине и конце. TTS transport-controls не доказывают приемлемость системного русского голоса; production-quality Russian TTS остаётся launch blocker при отсутствии приемлемого системного голоса.
- Auth/Account, настоящий книжный PDF, payment/subscription/AI balance и legal readiness по-прежнему обязательны как следующий baseline по PRD. PR #10 не готов к merge и не даёт оснований объявлять Beta готовой.

## 22.08.2026 — исправления после ручной Chrome-приёмки voice-first

### Исправляется в PR #10
- Ручной Chrome QA обнаружил, что прежний lifecycle мог создать аудиофрагмент до получения финального Speech Recognition result: пустая расшифровка блокировала сборку и уточняющие вопросы. PR ждёт final result/end и показывает восстанавливаемое понятное состояние при отказе микрофона или STT.
- «Расшифровать ещё раз» больше не маскирует ручной browser prompt под автоматическую операцию: без STT-провайдера показано честное ограничение, а «Исправить текст вручную» открывает inline editor и добавляет confirmed revision.
- Скрытие аудио стало обратимым; последующая корректировка D052 полностью исключила из интерфейса и модели пользователя формулировку и действие «мягкого удаления».

### Не является готовностью Beta
- Реальная авторизация/аккаунт, книжный PDF, платежи/подписка/AI balance и public-Beta legal readiness остаются обязательными следующими этапами. Они не считаются закрытыми заглушками текущего интерфейса.

## 22.08.2026 — безопасное завершение голосового baseline PR #10

### Добавлено
- Раздельный накопительный контур голосового ответа: каждый уточняющий вопрос получает свой `questionId`, ответ, аудиофрагменты, источники и версии расшифровки без замены исходного рассказа.
- Связи нескольких фрагментов одного ответа и выбранных transcript revisions; повторная ручная расшифровка сохраняет прежнюю ревизию и provenance.
- Явная маркировка browser transcript как черновика, требующего проверки; UI «Расшифровать ещё раз» и отображение текущей/непроверенной версии.

### Проверено
- TypeScript, Vitest (18 тестов), ESLint без новых предупреждений ветки и production build.
- Локальный browser QA: последовательные вопросы, сохранение и reload, TTS `Слушать → Пауза → Продолжить → Стоп → повторный запуск`, отсутствие горизонтального overflow на desktop 1440/1280 и mobile 390/360.

### Остаётся blocker публичного запуска
- Подключённый in-app browser не дал доступ к микрофону, поэтому фактическая STT-проверка трёх русских записей 30–60 секунд и voice E2E с двумя ответами не подтверждена. До публичной Beta требуется пройти её в браузере с рабочим `getUserMedia`; при неполной browser-расшифровке подключить отдельный production-quality `TranscriptionProvider` без удаления оригинальных аудио.

## 22.08.2026 — первый безопасный фундамент истории Beta

### Изменено
- Зафиксировано решение D046: аддитивные объекты истории, copy-first migration legacy `app_state`, отдельные версии истории и server-side optimistic concurrency.
- Техническая архитектура уточнена для D1-таблиц истории/источников/аудиофрагментов/версий, R2-оригиналов, обратимой миграции и запрета production fallback identity.
- Beta UX и PRD уточнены для последовательных аудиофрагментов, раздельных исходных материалов, версий расшифровки и инварианта отсутствия тихой потери данных.

### Не включено
- Платежи, юридические страницы, PDF, production-STT, глобальный визуальный редизайн и сложная retention-система не включены в этот PR: они не нужны для минимального безопасного фундамента.

---

## 22.08.2026 — первая работающая приватная Beta

- Будущее направление «У меня уже есть рукопись» закреплено за Этапом 2 без расширения текущего Beta-scope.
- Доменная модель совместимо обобщена до `Book → Chapter → Story → Source / Revision`; добавлено автоматическое преобразование сохранённого состояния v1 в v2.

### Добавлено
- `apps/web` — полноценный адаптивный сайт-приложение на React/TypeScript/Vinext/OpenAI Sites.
- Рабочий путь первой и последующих историй: голос/текст, один вопрос, безопасная сборка, происхождение, ручное исправление, регистрация после подтверждения и постоянное сохранение.
- «Моя Книга жизни»: чтение, browser TTS, повторная правка, вторая история, стиль, приватность, JSON/Markdown/print-PDF экспорт, AI-баланс, отдельная обратная связь и roadmap.
- D1 для структурированных данных и R2 для аудио; серверная привязка данных к пользователю приватного Site.
- Автотесты запрета выдумки, происхождения, Пробного периода и StorageAdapter; browser E2E и responsive QA.
- `15-TECHNICAL-ARCHITECTURE.md` — фактический baseline реализации и открытые production-решения.

### Изменено
- `README.md`, `03-DECISION_LOG.md`, `05-PRD.md`, `11-AI-ARCHITECTURE.md` и `99-INDEX.md` синхронизированы с реализованной Beta.
- Минимальный экспорт утверждён как JSON + Markdown + print/PDF.
- Голосовой baseline использует browser-native возможности с честным fallback при отсутствии распознавания.

### Не подключено намеренно
- Внешний LLM/STT, реальный платёжный процессор и production-инфраструктура хранения не имитируются; для них сохранены адаптерные границы и отдельные решения следующего этапа.

---

## 22.08.2026 — синхронизация паспорта с актуальной миссией и Beta-baseline

### Изменено
- `08-PRODUCT-PASSPORT.md` обновлён до v0.7 и приведён к действующим решениям `Roadmap v1.1`, `Decision Log v0.2`, `PRD v0.2` и `Beta UX v1.1`.
- В паспорт перенесена подтверждённая эмоциональная миссия: Книга жизни как тёплое личное место, добрая память о прожитом, опора для настоящего и долгосрочная ценность для человека, семьи и рода.
- Уточнено, что идеи силы духа и человека как творца собственной жизни выражаются через реальные истории выбора, ответственности, творчества, преодоления и развития, а не навязываются как универсальная доктрина.
- Текущий приоритет заменён на фактическую последовательность: полноценный визуальный вариант для компьютера и телефона → UX-проверка → выбор технической архитектуры/стека → первая платная Beta.
- Устаревшие требования раннего прототипа о локальном браузерном приложении, обязательном внешнем Читателе и обязательных игровых замках выведены из действующего baseline; они остаются доступными в истории Git.
- Формулировка `Trial` заменена на действующее пользовательское название «Пробный период».

### Причина
После переноса проектового канона в GitHub паспорт оставался на более ранней редакции и частично расходился с уже подтверждёнными решениями первой Beta и свежим эмоционально-визуальным направлением. Синхронизация устраняет второй смысловой слой внутри самого GitHub-канона без изменения дорожной карты.

---

## 21.08.2026 — подготовка к активной параллельной разработке сайта

### Добавлено
- `13-WORKFLOW.md` — единый рабочий процесс параллельных чатов/агентов: отдельные ветки, Pull Request как единица интеграции, локализация конфликтов, границы самостоятельности и связь с личной базой Ильи.
- `14-BETA-UX.md` — утверждённый UX-baseline первой Beta, перенесённый из свежего Google Drive-документа: путь до регистрации, голос/текст, ИИ-интервью, проверка истории, пробный период, единое пространство книги, мобильный/компьютерный паритет и визуально-эмоциональные критерии.

### Изменено
- `02-ROADMAP.md` обновлён до v1.1: GitHub закреплён как канон продукта/кода, удалена зависимость от «управляющего чата», добавлена ссылка на параллельный workflow.
- `03-DECISION_LOG.md` обновлён до v0.2: устранены устаревшие «открытые» вопросы о первом продукте/MVP и добавлены подтверждённые решения Beta от 18–21.08.2026.
- `05-PRD.md` обновлён до v0.2: вместо старого статуса «MVP не утверждён» сформирован действующий проверяемый baseline первой платной Beta.
- `99-INDEX.md` обновлён до v1.3: добавлены Beta UX и Workflow, уточнён порядок чтения и роли документов.

### Структурное решение
- Активная продуктовая/техническая документация KTOYA ведётся только в `AinSpiral/Ktoya`.
- Google Drive остаётся источником личных данных Ильи, архивов и системной навигации, но не вторым проектным каноном.
- Технологический стек и папки кода не создавались заранее: это следующее решение после визуальной проверки UX.

---

## 21.08.2026 — консолидация Google Drive → GitHub

### Добавлено
- `08-PRODUCT-PASSPORT.md` — расширенный подтверждённый паспорт продукта, ранее развивавшийся на Google Drive.
- `09-METHODOLOGY.md` — методология сбора и обработки жизненного материала.
- `10-SOCIAL-ARCHITECTURE.md` — детальная социальная архитектура.
- `11-AI-ARCHITECTURE.md` — детальная архитектура и границы роли ИИ.
- `12-GAME-SYSTEM.md` — игровая/пазловая система с актуализацией по Roadmap.

### Изменено
- `02-ROADMAP.md` был заменён более свежей утверждённой baseline от 18.08.2026: первая платная Beta, пробный период, один тариф с AI-балансом, private-by-default, этапность от Beta до цифрового наследия и отказ от обязательных игровых замков по умолчанию.
- `99-INDEX.md` закрепил GitHub `AinSpiral/Ktoya/docs` как единый канон продуктовой документации.

### Причина
На Google Drive после документации GitHub от 15.08.2026 появились более свежие подтверждённые решения. Два параллельно редактируемых комплекта создавали конфликт источников истины. Личные истории и другие личные данные Ильи остаются в личной базе знаний, а не дублируются в репозитории.

---

# [Документация 1.1] — 2026-08-15

## Добавлено
- `02-ROADMAP.md` — первая поэтапная дорожная карта.
- `03-DECISION_LOG.md` — журнал решений и открытых вопросов.
- `04-IDEAS.md` — банк идей.
- `05-PRD.md` — первоначальный концептуальный PRD.
- `06-GLOSSARY.md` — словарь терминов.
- `07-CHANGELOG.md` — журнал изменений.
- главы 07–25 в `01-BIBLE.md` — игровой слой, безопасность, хранение, автономность, семьи, социальный граф, карты, поиск, рекомендации, будущие технологии и архитектура платформы.

## Уточнено
- решения отделены от гипотез, идей, открытых вопросов и исследований;
- ответственность человека обозначена как мировоззренческий принцип, а не универсальный научный факт;
- психологические типологии — модели, а не диагнозы;
- духовные/необычные явления — опыт и интерпретации Автора;
- нейроинтерфейсы — долгосрочное исследование;
- приватный режим требует отдельного технического проектирования.

---

# Исходная документированная база

До документации 1.1 в репозитории существовали `README.md`, `00-CONSTITUTION.md`, ранняя `01-BIBLE.md` и `99-INDEX.md`. Точная последовательность ранних редакций не восстанавливается предположительно; история Git остаётся источником старых версий файлов.
